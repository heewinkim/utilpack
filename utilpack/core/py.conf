# 모든 값들은 str 로 처리되며, lowcase 사용을 권고 합니다. (eg. True(x) true(o) )
# list형의 경우 콤마를 통하여 표현되며 이 역시 데이터 상으로는 str으로 인식됩니다.

[LOG]
PATH = /opt/py/log
ROTATE = false

[FLUENTD]
IP = 0.0.0.0
PORT = 12510
TAG = py.ai

[SLACK]
CHANNELS = py
TOKEN = put-token